{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "buildCommand": "npm install && npx vite build",
  "outputDirectory": "dist",
  "env": {
    "VITE_BASE_PATH": "/Test"
  }
}
